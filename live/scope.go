package live

const (
	// ScopeGrafarg contains builtin real-time features of Grafarg Core.
	ScopeGrafarg = "grafarg"
	// ScopePlugin passes control to a plugin.
	ScopePlugin = "plugin"
	// ScopeDatasource passes control to a datasource plugin.
	ScopeDatasource = "ds"
	// ScopeStream is a managed data frame stream.
	ScopeStream = "stream"
)
